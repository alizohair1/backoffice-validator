# Optional. Without this, the app works fully — you just won't see the
# "Get AI suggestions for the rest" button do anything useful (it'll show
# a clear error instead of silently failing).
#
# Get a free key at https://console.groq.com/keys (no credit card needed).
GROQ_API_KEY=
